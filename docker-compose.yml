services:
  weather-trader:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: polymarket-weather-trader
    restart: unless-stopped
    environment:
      SIMMER_API_KEY: ${SIMMER_API_KEY}
      INTERVAL_SECONDS: ${INTERVAL_SECONDS:-60}
